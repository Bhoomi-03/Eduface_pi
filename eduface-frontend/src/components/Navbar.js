// Thin compatibility wrapper so imports of './components/Navbar' continue to work.
// The JSX implementation lives in Navbar.jsx to avoid parser issues in some environments.
export { default } from './Navbar.jsx';
